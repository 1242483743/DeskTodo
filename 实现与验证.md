# 实现与验证

## 1.22 图标占比、Shell 引用与安装页

- 保留用户指定的透明源 PNG，不改图案或软件内 PNG。ICO 导出时按主体边界取景并居中等比缩放，去掉多余透明留白；16、20、24、32、40、48、64、96、128、256 十种尺寸的透明四角和至少 90% 中线图案占比均验证通过。
- 小尺寸采用 32 位 DIB 和基于 alpha 的 AND 透明遮罩；仅 256px 使用 PNG 压缩。参考 [微软图标设计指南](https://learn.microsoft.com/en-us/windows/win32/uxguide/vis-icons) 和 [微软 ICO 格式说明](https://devblogs.microsoft.com/oldnewthing/20101018-00/?p=12513)。这是 ICO 尺寸布局及格式导出，没有重新生成或修改源 PNG 像素。
- 安装器部署 `DeskTodo-v1.22.ico`，开始菜单、桌面快捷方式和卸载注册入口明确引用该文件，使用新的缓存键；安装后对自己的程序和快捷方式通知 Shell，不删除全局缓存、不结束或重启 Explorer。
- 安装页图标改为指定清单图案的 256px 透明帧，移除“已有待办、主题和提醒会保留”说明行，卸载页仍说明数据保留位置。已检查安装第一页渲染及 48px 图标预览。
- 软件 197 项、安装器 47 项检查通过。安装器在独立测试目录真实创建快捷方式，并通过 SHGetFileInfo 提取开始菜单和桌面图标，验证四角透明及图案占比；增加图标载荷、明确快捷方式引用和图标失败回滚检查。注册表仍模拟，不安装到真实 Program Files；用户已经固定在开始菜单的旧入口与真实安装后的桌面需覆盖安装后确认。
- 软件内 PNG 与透明源 PNG 的 SHA256 均与 1.21 完全一致。发布载荷无个人待办文件，不修改用户已有待办数据。

## 1.21 桌面图标透明背景

- 内置图像工具背景提取，提示要求：仅去掉原 ICO 圆角图案外侧的白色方形画布，保留清单、勾选框、加号和原配色，不修改软件内 PNG。选择第一版透明结果，第二版边缘改善不明显未采用。
- 透明源图保存到 `图标/待办软件图标-透明.png`，`build-icon.ps1` 只执行保留 alpha 的缩放及 ICO 格式转换，输出 16、24、32、48、64、128、256 七种尺寸，各帧四角透明验证通过。已检查 48px 桌面尺寸预览，没有方形白底。
- EXE 自检增加 Windows ExtractAssociatedIcon 实际提取图标的透明四角验证；软件 197 项、安装器 36 项检查通过。安装器通知 Shell 更新已写入的程序和快捷方式图标，不结束或重启 Explorer。安装器载荷不包含个人待办数据。

## 1.20 托盘崩溃修复与两页安装向导

- 复现旧托盘菜单在关闭期间再次 Close 的 InvalidOperationException，与用户截图中的错误一致。加入 Closing/Closed 状态和幂等关闭；失焦延后关闭，动作在 Closed 完全返回后由 Dispatcher 执行。关闭后不再 Show、EnsureHandle、Activate，替换菜单时先移除控制器引用。
- 主窗口取消 Closing 时也不在该事件中 Hide，延后处理；控制器 Dispose/Exit 可重复调用，退出后排队动作不继续执行。
- 安装第一页可选本机路径，默认系统 Program Files\DeskTodo；第二页可勾选开始菜单和桌面快捷方式。删除旧说明行。只有需要写入受保护目录时提权后台工作进程，保留原用户 SID、数据、快捷方式和注册表位置，完成页启动软件不继承工作进程管理员权限。
- 卸载从当前安装目录取明确目标，临时卸载器和提权工作进程均传递路径；禁止磁盘根目录、数据目录及其父子路径、系统根目录和符号链接。仅删除已知程序文件，不递归删除任何目录。
- 通过 196 项软件检查、36 项安装器检查。可见菜单执行显示、隐藏托盘、切换模式、设置、退出，以及重复关闭、关闭时再关闭和关闭后 Show。安装器实际检查两页切换、中文/空格路径、四种快捷方式组合、失败回滚、卸载保留数据；注册表模拟，不安装到真实 Program Files。真实 UAC/不同管理员账号流程仍需用户批准后手动验证。
- 已检查第一页、快捷方式页、完成页及卸载页渲染，程序与内置图标用途保持不变。发布包不包含个人待办数据。

## 实现

C# 5 / .NET Framework 4.8 / WPF，x64，无第三方包。Windows 系统编译器可直接编译；`src/DesktopTodo.csproj` 也可在安装了 .NET Framework 开发工具的 Visual Studio 中打开。

最终桌面模式采用图标表面内的子窗口托管：

- 动态查找 Explorer 的 `SHELLDLL_DefView` 容器，兼容 `Progman` / `WorkerW` 布局。
- 将交互窗口作为桌面图标表面 `SysListView32` 的子窗口；图标表面不可见时回退到 `SHELLDLL_DefView`。
- 不将卡片放在图标表面的后面，不用 `HWND_TOPMOST`，没有 Win+D 的隐藏后补显示或 Z 顺序轮询。
- 点击时为自身 HWND 设置输入焦点，然后由 WPF 路由到输入框。
- 每 2s 检查宿主丢失、显示/退出请求和失败保存重试；正常 Win+D 不触发重新挂载。

第一版挂在 `Progman` 层，系统报告成功，但用户看不到；父句柄正确不等于实际可见。

随后尝试顶层桌面跟随：仅基于焦点的版本在 Win+D 后会消失，参考窗口加临时层级的版本可见但会闪一下，均不符合用户的连续显示要求。最终移除补显示方案，改为直接嵌在图标表面内部。

相关主要资料：[微软 WPF/Win32 互操作](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/advanced/wpf-and-win32-interoperation)、[微软 SetParent 的跨进程及 DPI 注意事项](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setparent)、[Rainmeter 桌面层级与窗口定位实现](https://github.com/rainmeter/rainmeter/blob/master/Library/System.cpp)、[Windows 11 24H2 桌面布局修复说明](https://github.com/rainmeter/rainmeter/pull/413)。代码独立实现，未复制 Rainmeter 源码。

## 1.1 修复

- 原缩放每次都累加 Thumb 的相对位移，同时写 WPF Width/Height 和原生坐标，还反复使用 SWP_FRAMECHANGED。现在使用固定起点的屏幕鼠标差值，以 CompositionTarget.Rendering 合并每帧请求，仅用原生 SetWindowPos 调整尺寸，不在拖动中保存磁盘或重建任务列表。
- 使用 WM_WINDOWPOSCHANGING 防护，保持嵌入窗口的屏幕位置/父客户坐标转换，阻止 WPF 的屏幕坐标缓存意外重定位子窗口。[微软消息说明](https://learn.microsoft.com/en-us/windows/win32/winmsg/wm-windowposchanging)
- 取消整卡 DropShadowEffect，避免透明窗口缩放时重复对整棵内容做阴影离屏渲染；仍保留圆角及浅色边框。
- 缩放手柄从内容 Grid 的负边距区域移到卡片根布局内部，点击范围扩大为约 26 DIP。
- 不再对已显示的 WPF Window 实时 Attach/Detach 切换角色。记住原生屏幕几何后重建对应模式窗口，提前确定 ShowInTaskbar；事件捕获各自窗口实例，避免旧 HWND 的回调操作新窗口。保留输入草稿、撤销栈、折叠状态和设置页面，失败回退普通窗口。
- 设置从系统 ContextMenu 改为卡片内部页面，主题、字体、圆角与主界面共用资源；事项菜单和编辑按钮也统一样式，滚动条去掉系统箭头。

## 1.2 跟手性和锁定图标

- 缩放不再等待下一次 CompositionTarget.Rendering：Thumb 的每次拖动事件立即调用一次 SetWindowPos，移除人为增加的一帧延迟。
- 1.2 曾改用普通合成窗口和原生圆角区域以改善缩放；1.3 根据实际边缘质量反馈撤销了像素区域裁剪，改用下节的抗锯齿方案。即时 SetWindowPos、无阴影和不等待下一帧的性能修复继续保留。
- 锁定按钮使用 Segoe MDL2 的闭锁/开锁两个不同字形。两种状态背景始终透明，悬停只改变图标颜色，不再出现会被误认为状态的外围框；工具提示和辅助功能名称也明确写出当前状态。

## 1.3 圆角和卡片内编辑

- 移除 `SetWindowRgn/CreateRoundRectRgn` 的整数像素裁剪。该方式缩放期间会临时变成方角，完成后边缘也会出现明显台阶。改为 WPF 每像素透明度直接绘制圆角，保留 1 DIP 透明边距和抗锯齿边缘；缩放仍然立即处理鼠标事件，不恢复等待一帧的逻辑。
- Windows 11 的 DWM 圆角 API只针对合适的顶层窗口；桌面模式的卡片是 Explorer 图标表面内的子窗口，因此不把 DWM 提示当作此模式的圆角方案。参考：[微软 Windows 11 桌面应用圆角说明](https://learn.microsoft.com/en-us/windows/apps/desktop/modernize/ui/apply-rounded-corners)。
- 锁按钮设置 `FocusVisualStyle=null` 且退出 Tab 顺序，Alt/键盘导航不再画像素虚线焦点框；状态仍可由鼠标点击，辅助功能名称仍明确说明开锁/闭锁。
- 删除旧的独立编辑 Window；编辑现在是主卡片内的同主题页面，含圆角输入区域、字数提示、取消/保存按钮、Esc 和 Ctrl+Enter 快捷键。它不再出现系统标题栏或缺失图标的问题。

## 1.4 加号状态和缩放预览

- 加号不再使用字体字形和负上边距；改为两条精确居中的圆角几何线，因此不受字体基线影响。它使用独立按钮模板，悬停和按下只调整透明度，不再被通用图标样式替换成灰色。
- 加号按钮不接收键盘焦点并移除焦点视觉。空输入点击只把输入焦点放回文本框，不添加事项、不进入持续灰态，按 Alt 也不会出现像素虚线框。
- 开始缩放时把当前卡片渲染为冻结的抗锯齿位图，拖动期间隐藏完整控件树并只拉伸该快照；缩放手柄独立保留以持续捕获鼠标。松手后立即恢复实时控件并释放位图，从而避免每个鼠标事件都重排任务列表、编辑区和设置区。

## 1.5 全局焦点装饰和矢量对齐

- 在窗口资源级覆盖 WPF 默认的虚线焦点装饰，并为按钮、复选框、设置开关、文本框、菜单项、折叠栏、滚动条及缩放手柄显式禁用焦点框。运行时还会在键盘焦点进入任何后生成控件前清除其 `FocusVisualStyle`，覆盖动态事项行和模板内部控件。
- 加号按钮改为 32×32 DIP、8 DIP 圆角并关闭像素吸附；输入条上下内边距与按钮同步。加号的横竖线固定在同一 18×18 网格中心，不受字体基线和负边距影响。
- 顶部省略号、事项纵向省略号、已完成展开箭头全部改为居中的 Ellipse/Path，不再使用 `···`、`⋮`、`›` 等字体字符或负上边距。
- 设置开关改为 36×20 DIP 抗锯齿轨道和独立 16×16 Ellipse 滑块，显式垂直居中并保留 2 DIP 四周间距；滑块不再作为圆角 Border 的嵌套子边框，因此不会与轨道右圆角粘连或被裁掉。

## 1.6 跨屏拖动与 Explorer 恢复

- Windows 错误报告记录到 `AppHangXProcB1`，参与进程分别为 `explorer.exe` 和 `桌面待办.exe 1.5.0.0`。故障屏幕为混合 DPI、含负坐标的双屏布局；旧版在每次鼠标移动时同步调用 `ScreenToClient(Explorer)` 和 `SetWindowPos` 移动 Explorer 的子窗口，形成了高频跨进程窗口操作，并在跨屏时触发 Explorer 与应用互相等待。
- 标题拖动和右下角缩放现在先把卡片渲染为冻结位图，放到本进程拥有的无激活、鼠标穿透顶层预览 HWND 中。鼠标移动只更新这个预览 HWND；真实桌面子窗口保持静止，松手后只更新一次。跨到另一显示器时保存目标屏幕坐标并重建 WPF HWND，不把原有分层子窗口直接拖过 DPI 边界。
- 应用清单从 System DPI-aware 改为 `PerMonitorV2, PerMonitor`，WPF 在 125% 与 175% 等不同缩放屏幕间重新计算比例，逻辑宽高仍保存在 DIP 中，原生位置保存在虚拟桌面屏幕像素中。
- Explorer 桌面宿主句柄变化或失效时不再从旧子窗口回写几何；控制器沿用最后一次成功保存的屏幕坐标，在新 `SysListView32` 宿主下创建新的 WPF HWND。显示配置变化也会先验证当前挂载关系，避免操作已经失效的父窗口。
- Explorer 刚重启而 `Progman/WorkerW` 桌面图标宿主尚未出现时，保持用户的“嵌入桌面”偏好，但先创建带任务栏入口的普通窗口并显示“等待桌面恢复”。2 秒检查发现宿主恢复后自动重建为桌面子窗口，避免启动后只有进程、没有可见卡片的假闪退。

## 1.7 拖动首帧与自适应字号

- 原预览是第二个 WPF Window，在主屏创建后移动到副屏时会自动应用 DPI 建议尺寸；125% 到 175% 的比例为 1.4，与用户截图中的突然放大一致。改用本进程创建的原生分层 HWND，冻结位图和位置均采用物理像素，忽略仅针对预览的 WM_DPICHANGED 自动缩放。真实交互窗口仍保留 Per-Monitor V2 缩放。
- 原生预览先用 UpdateLayeredWindow 填充完整的预乘 Alpha 位图，再显示并隐藏真实卡片；松手后等待真实 WPF 内容完成布局/渲染再销毁预览，减少画面空档。仅按住标题、未超过系统拖动阈值时不启动预览，也不隐藏内容或提交几何。
- 原生预览的创建、更新、显示器枚举和屏幕/父客户坐标转换临时使用物理 DPI 上下文，并立即恢复调用方上下文，避免不同进程/宿主 DPI 感知造成坐标虚拟化。
- 内部内容容器根据窗口宽高使用统一 LayoutTransform，比例为 max(1, min(1.8, width/360, height/400))，主界面、设置与编辑中的字体、图标、按钮和间距同步放大；进度条按实际内容轨道宽度计算，避免变换后溢出。
- 参考：[微软 UpdateLayeredWindow](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-updatelayeredwindow)、[线程 DPI 上下文](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setthreaddpiawarenesscontext)、[WM_DPICHANGED](https://learn.microsoft.com/en-us/windows/win32/hidpi/wm-dpichanged)。

## 1.8 编辑、提醒与主题

- 事项行将完成 CheckBox 与文字区域拆成独立列。只有 30×38 DIP 的左侧勾选区域绑定完成事件；未完成文字的双击事件只打开卡片内编辑器，单击文字、行空白和菜单按钮不会改变完成状态。
- 编辑页新增“展开编辑/恢复大小”。展开时记住原窗口位置和尺寸，在当前显示器工作区内扩大卡片，并将编辑内容比例限制为 135%，把更多空间留给多行文本；保存、取消或主动恢复后还原原几何。
- 数据模型新增可选提醒字段，旧版 JSON 缺少字段时默认关闭，不会意外在午夜提醒。调度器支持 1–168 小时间隔、每天本地指定分钟、同日去重和十分钟稍后提醒；没有未完成事项时不弹窗。
- 提醒使用独立无系统标题栏的圆角卡片，复用当前主题并展示最多四项未完成事项。它不托管在 Explorer 桌面层，因此其他应用打开时也能看到。
- 主题抽成统一调色板：森屿、海盐、雾紫、陶粉、琥珀各包含完整的浅色和深色七色资源，主卡片、设置、菜单和提醒窗口同步使用。

## 1.8.1 事项文字对齐与编辑简化

- 将事项文字的顶部字体留白补偿增加 2 DIP，使单行数字、中文和多行事项的首行可见字形与勾选框、纵向省略号的中心对齐，仍保持文字左对齐和独立点击区域。
- 删除“展开编辑”按钮、编辑扩展状态、临时窗口几何缓存及放大/恢复方法，不再因编辑操作改变窗口大小；保留现有的完整卡片内编辑区域。
- 使用单行 `123`、中文和三行文本渲染检查对齐，生成 `qa/task-alignment.png`，简化编辑页为 `qa/edit-no-expand.png`。

## 1.9 独立事项提醒与可编辑标题

- 提醒规则、上次间隔触发、每日去重日期和稍后提醒时间均改为 TodoItem 字段；调度器只遍历未完成事项。旧版 AppState 全局字段仅为兼容旧 JSON 保留，不调度、不自动复制规则。
- 事项右侧菜单加入“设置提醒”，采用卡片内部的同主题页面；支持小时与分钟组合间隔、每日 HH:mm、清除提醒、取消草稿和 Ctrl+Enter 保存，输入验证全部成功后才原子修改两条规则。删除设置页全局提醒控件及其事件处理。
- 同时到期的多条事项串行弹窗，每次只显示一条；完成、删除或清除规则后关闭对应活动弹窗。长内容在固定高度内滚动，按钮不会被正文挤出；滚动条和焦点装饰与主体统一。
- 提醒输入采用弹性列宽，数字与单位始终成组。输入框模板去除重复内边距，避免单行数字裁切；最小卡片内内容滚动，底部清除、取消和保存固定可见。已渲染检查默认、320×360 和 720×1100 尺寸。
- 主页标题支持双击原位编辑，Enter/失焦保存、Esc 取消，空值不覆盖，最长 12 字并用省略号保护右侧操作区；旧数据缺少标题时默认“待办”。标题及独立提醒随原 JSON 保存，宿主重建保留提醒草稿。

## 1.10 备份、批量完成项、时间与设置弹层

- 设置页底部动态读取程序集版本号。新增备份导入：选择 JSON 后先通过完整模型校验，再在同主题弹层中显示未完成/已完成数量并二次确认；实际替换前在数据目录额外生成 `before-import-*`，导入只恢复清单和外观，保留本机窗口几何与桌面模式。
- 已完成标题右侧新增矢量垃圾桶。清空先显示卡片内二次确认，确认后只做软删除；整批作为一个撤销组，因此一次撤销即可恢复所有刚清空的事项。单条删除和跨宿主重建继续保留撤销顺序。
- 事项行在纵向菜单左侧显示 `HH:mm`：未完成读取 UTC 创建时间并转换到本地，已完成读取完成时间；设置用反向 `HideItemTimes` 字段，使旧 JSON 缺少字段时仍默认开启。
- “重置位置大小”改为带竖向三点的入口，同主题弹层提供四个角落与 75/100/125/150% 预设。实际大小受原 320×360 至 720×1100 安全范围约束，并基于当前显示器工作区和物理 DPI 定位；最小窗口时弹层可滚动。
- 编辑框失去卡片内焦点或点击编辑页其他区域时直接保存；取消按钮在焦点切换前设置抑制标志，确保取消不会被失焦保存抢先执行。
- 间隔小时、分钟和每日 HH:mm 输入右侧增加无焦点矢量上下箭头；小时/分钟空值按 0 处理，因此可只设分钟。每日时间按一分钟循环。输入验证仍先整体成功再修改规则，避免部分保存。
- 设置反馈、导入结果、导出结果与清空确认复用卡片内圆角遮罩弹层；最小、默认和大尺寸均使用主体主题、焦点样式和滚动条。

## 1.11 默认偏好、圆角与交互细化

- 设置中的“默认位置大小”拆成主体按钮和最右侧独立竖三点。主体按钮直接应用已经保存的默认位置/比例，竖三点只负责打开偏好面板；面板保存只更新偏好，不会让卡片当场移动。比例扩展为 75%–250% 共八档，并以带风景画面的电脑矢量图展示四角位置。
- 主卡片按实际尺寸使用抗锯齿 `RectangleGeometry` 裁剪，遮罩延伸覆盖卡片边线后再受同一圆角裁剪，避免设置弹层出现直角外框或一圈白边。
- 事项菜单固定使用与待办文字一致的 13 DIP 字号，不再叠加内容缩放；已完成清空改为简洁系统矢量垃圾桶。清空或删除后的撤销记录由 30 秒计时器统一过期，宿主重建时保留剩余有效时间。
- 位置锁定时禁止主页标题进入编辑，并同步鼠标提示；事项编辑的点击外部保存提升到窗口级预览鼠标事件，编辑页内外的可点击区域均能确认修改，取消按钮仍优先取消。

## 1.12 全屏默认偏好页面

- “默认位置与大小”不再显示在半透明遮罩上的居中弹窗中，打开后以不透明同主题页面覆盖整张卡片；确认、提示和清空二次确认仍使用居中圆角弹层。
- 电脑示意图从宽扁屏幕调整为更接近日常显示器的比例。风景壁纸使用固定圆角 `RectangleGeometry` 单独裁剪，山景不会漏到直角区域；四个定位圆点向屏幕内部留出 10 DIP，完整显示且不贴边。

## 1.13 对齐、圆角与位置反馈

- 事项右侧菜单的工具提示改为固定出现在按钮左侧并向左偏移，避免桌面子窗口右边缘裁掉“编辑、提醒或删除”文字。
- 位置页关闭键不再使用受字体基线影响的 `×` 字符，改为固定 18×18 网格内的矢量交叉线；“恢复默认”和“保存”明确使用相同高度、零外边距和顶部对齐。
- 电脑外框、壁纸及壁纸裁剪统一为 16 DIP 圆角，并关闭像素吸附以保留平滑边缘。四个位置圆点从边缘向内 14 DIP，使用白底主题色描边；选择左上、右上、左下或右下时，对应圆点立即填充当前主题色。

## 验证记录

- `build.ps1 -Test`：164 项自检通过；报告位于 `qa/test-report.txt`。
- 模式压力测试使用自有窗口模拟宿主：12 次切换检查可见性、几何、输入草稿、撤销栈和设置状态；80 次尺寸更新检查左上角固定、精确尺寸及 WPF 内容同步，额外检查异常坐标回写、隐藏再显示、挂载失败回退。没有主动操作真实 Explorer 的生命周期。
- 1.1 已启动真实桌面的独立修复验证版，并通过设置开关切入桌面模式；诊断显示 SysListView32 / child=True / visible=True / topmost=False，位置尺寸在切换前后相同。嵌入后窗口不在自动化工具的顶层窗口列表中，实际连续拖动手感和新版本 Win+D 仍需用户手动复验，不以句柄诊断替代用户可见性确认。

## 1.14 程序图标

- 使用项目根目录的 `待办软件图标.ico` 编译为 exe 的 Win32 图标；运行时从当前 exe 提取同一图标用于 WPF 窗口和系统托盘，提取失败才回退到内置绘制图标。
- 图标不作为外部依赖放入发布包，仍保持 `使用说明.md`、`桌面待办.exe`、`桌面待办.exe.config` 三个文件即可运行。
- 自动化工具在 125% DPI 下的截图/拖动坐标未能稳定触发缩放手柄，故不把这些拖动尝试计为实测通过；缩放算法、原生几何及点击命中范围通过独立自检验证。
- 1.2 的独立窗口测试版已由用户手动验证：开锁/闭锁图形切换清楚且无外围框，拖动缩放能紧跟鼠标、无明显延迟。
- 已检查浅色、深色、已完成展开、最小尺寸、滚动列表及设置面板状态的 PNG。
- 已在真实 Windows 桌面执行回车添加、点击勾选、已完成展开，并读取独立测试 JSON，确认完成状态落盘。
- 直接嵌入版诊断确认：`hostClass=SysListView32`、`child=True`、`visible=True`、`topmost=False`。
- 未自动发送 Win 键：computer-use 技能禁止使用 Win 键快捷键；Win+D 的稳定可见性及嵌入后的中文输入通过用户手动确认。
- 用户已确认直接嵌入版按 Win+D 不会闪烁。最终交付采用此实现，已移除补显示计时器。
- 未启用真实开机自启，也未通过重启电脑测试。启动项命令格式已验证，开关由用户手动开启。
- 1.6 的宿主恢复测试会在测试进程内创建两个 Explorer-like 顶层宿主，替换桌面表面句柄后验证旧窗口被重建、保存坐标不被失效父窗口覆盖，并确认新窗口已挂载到替代宿主；测试不结束或重启真实 Explorer。
- 1.7 在两块真实显示器上分别创建本进程的原生预览，验证首次显示及布局空闲后的物理尺寸精确；合成发送 175% WM_DPICHANGED 建议矩形后仍保持尺寸；每块屏幕连续 80 次移动/缩放均检查位置和尺寸，并验证调用方 DPI 上下文恢复。未自动拖动真实 Explorer 子窗口。
- 1.7 模拟标题左键按下但不移动，确认不生成预览、不隐藏卡片、不改变窗口尺寸；检查大卡片 180% 内容比例、进度轨道、默认/短卡片恢复，并渲染检查大尺寸主界面、设置和编辑页面。两块屏幕上的实际按住/拖动手感仍需用户手动复验。
- 1.8 检查勾选框与文字命中区域分离、单击不完成、双击进入编辑、展开后恢复原几何；验证主题及提醒字段序列化、间隔到期、每日去重、十分钟稍后提醒，并渲染五色主题设置、扩展编辑和提醒弹窗。真实系统时间到点后的可见提醒仍需用户设置一个临近时间手动复验。

## 1.15 DeskTodo 命名、顶部拖动与外观扩展

- 程序、程序集、窗口、托盘及发布文件统一改名为 `DeskTodo`；沿用 `%LOCALAPPDATA%\DesktopTodo` 数据目录，确保升级不迁移、不覆盖用户清单。旧的自启命令若指向 `桌面待办.exe`，正式 `DeskTodo.exe` 启动时自动更新为新路径。
- 顶部图标改为圆角底色上的三行清单矢量，默认名称改为 DeskTodo；旧数据中仍为默认“待办”的标题会迁移为 DeskTodo，已有自定义标题保持不变。
- 取消标题双击原位编辑，名称编辑移入设置页。卡片上沿至输入条上方的整个区域统一命中移动逻辑；右侧按钮仍排除在移动命中之外。锁定时名称输入和移动/缩放均禁用。
- 事项菜单增加“复制事项”，同一个菜单同时用于右侧三点和整行右键。设置新增可选最小化与关闭按钮，两个开关默认关闭；关闭图标使用卡片内二次确认。
- 新增鼠尾草、靛青、青黛、暮玫、可可、岩灰、霁蓝七套主题，合计十二套，每套均提供七项浅色与深色资源。深色位置预设按钮显式绑定主题文字色。

## 1.16 设置分组、主题网格与独立页首图标

- 设置页将“桌面”改为“基础功能”，将“界面按钮”改为“自定义界面”；事项时间与新增的“显示月日”选项统一归入自定义界面。月日开关仅在事项时间开启时可用，关闭时显示 `HH:mm`，开启时显示 `M/d HH:mm`，并随备份保存与导入。
- 十二套主题改用固定三列四行的等宽网格，三字主题名称不会再改变列宽或打乱对齐。
- `待办列表图标.ico` 作为独立资源仅加载到主界面左上角。EXE 的 Win32 图标、窗口标题栏、任务栏及托盘仍由原 `待办软件图标.ico` 提供，发布包不需要携带任何外部 ICO 文件。

## 1.17 内部图标、提醒对齐与拖动交接

- 主界面与提醒弹框改为共用 `图标\待办软件内图标.png`。载入后等距裁掉 10% 的透明画布，让 30 DIP 与 42 DIP 两种显示尺寸都保持清晰可辨；EXE、窗口、任务栏和托盘图标仍使用原来的 `待办软件图标.ico`。
- 提醒事项容器使用明确的最小高度和垂直居中滚动内容，单行事项的几何中心与圆角框中心一致，长内容仍可在 220 DIP 内滚动。
- 原生拖动预览首次显示后调用 `DwmFlush`，确认桌面合成器已经呈现冻结画面，再让实时卡片透明；松手后在 WPF 卡片完成最终位置渲染并由合成器提交后才销毁预览，缩短两种画面交接时可能出现的空帧。

## 1.18 页首按钮视觉中心

- 最小化图标不再使用靠近 16 DIP 容器底部的横线，而改为无外边距的固定矢量横线，并同时以容器和按钮为基准水平、垂直居中。关闭叉号也加入相同的中心坐标回归检查，确保标题栏按钮保持一致。
- 添加框获得键盘焦点时直接隐藏占位提示，失焦后再根据内容恢复，避免光标与提示字重合。
- 新安装默认锁定，默认偏好为右上角 100%。自启命令追加 `--startup`，通过自启登录时强制锁定；恢复上次保存的屏幕坐标与尺寸。Explorer 暂时回退普通窗口时不再回写临时几何，宿主恢复后使用最后一次有效位置。
- 构建脚本改为 ASCII 文本并自动识别项目图标/说明文件，避免 Windows PowerShell 5 将无 BOM 中文脚本误判编码。发布文件为 `DeskTodo.exe`、`DeskTodo.exe.config`、`使用说明.md`。

独立测试清单位于 `qa/ui-session` 和 `qa/desktop-session`。其中用户测试时输入的内容予以保留，不会被自检清空。

## 1.19 按修改文档改进及安装器

- 已读取 `DeskTodo修改点.docx` 全部文字，并检查三张嵌入截图。清空按钮与分隔线之间增加 10 DIP 留白，已完成折叠标题与按钮一起居中；事项右侧的删除按钮替换原菜单，不再提供已完成事项编辑、复制及提醒菜单。
- 删除和整批清空提示改为 15 秒倒计时，每 250 毫秒仅更新提示文字，不重建列表；到期删除提示及撤销记录。单条、批量撤销保持原完成状态，宿主重建保留剩余期限，不把期限重置。
- 开闭锁、垃圾桶、关闭、折叠箭头和数字微调箭头使用 1.8 DIP 线宽，跟最小化一致；加号使用同宽的几何线条。原应用 ICO 和内部 PNG 的用途不变。
- 托盘右键改为当前主题配色的 WPF 圆角菜单，提供显示、隐藏托盘、嵌入 / 取消嵌入桌面、设置与退出。设置新增托盘显示开关；隐藏托盘时保证卡片可见，随后隐藏或最小化卡片会恢复托盘，避免失去全部入口。
- 新增单文件、按用户安装的 EXE 安装器。默认安装路径 `%LOCALAPPDATA%\Programs\DeskTodo`，默认加入开始菜单，安装完成后选择桌面快捷方式与立即启动；添加当前用户“已安装的应用”卸载入口。保留已有自启状态，更新已启用的路径，不擅自启用自启。
- 升级先请求运行实例保存退出，以原子替换写入程序，失败恢复原文件和注册状态；卸载需要确认，只移除固定的四个程序文件及自己的快捷方式、注册项。数据、备份和无关文件不删除，也不递归清空安装目录。
- 共通过 185 项软件检查及 18 项安装器检查。软件自检覆盖原有功能及新增显示间距、线宽、直接删除、真实倒计时、托盘主题与模式切换、隐藏后的恢复入口；托盘菜单在两块真实显示器的任务栏附近打开，验证混合 DPI 下完整显示。安装器测试在独立文件夹中实际执行安装按钮、STA 后台安装、COM 快捷方式、升级、失败回滚和卸载；注册表使用模拟实现，不改真实系统设置。新增自启路径迁移检查，目录改名后只修复失效的软件启动路径。
- 已渲染检查主界面、已完成展开、倒计时、浅色 / 深色托盘菜单、安装首屏 / 完成页 / 卸载确认和最小尺寸卡片。真实 Explorer 的 Win+D 与混合 DPI 手感没有通过自动按键测试，仍以已有用户反馈及手动复验为准。
